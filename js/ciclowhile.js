var contador = 0;
while (contador < 10) {
    console.log("Vuelta numero " + contador);
    contador = contador + 1;
}
//tenemos que parar el bucle con contador es igual a contador mas 1 para subir su valor por cada vuelta hasta llegar al 10 y parar, si no ponemos eso se vuelve infinito
